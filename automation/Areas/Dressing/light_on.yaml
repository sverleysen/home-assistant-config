- alias: dressing_lights_on
  description: Doe het licht aan in de dressing aan als er beweging is
  trigger:
    - platform: state
      entity_id:
        - binary_sensor.dressing
    - platform: state
      entity_id:
        - sensor.dressing
  condition:
    - condition: state
      entity_id: input_boolean.motion
      state: 'on'
    - condition: state
      entity_id: input_boolean.motion_dressing
      state: 'on'
    - condition: state
      entity_id: binary_sensor.dressing
      state: 'on'
    - condition: template
      value_template: >-
        {{states('sensor.dressing')|float <= states('input_number.motion_dressing_lux')|float }}
  action:
    - service: light.turn_on
      data:
        entity_id:
          - light.dressing